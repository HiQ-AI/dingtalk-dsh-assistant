// Web host face: mounting this entry lets dsh-client-modules include ./client
// in __DSH_BOOT__. The resident/DWS process is mounted through ./resident.
export const name = 'dingtalk-dsh-assistant-web'

export function apply() {}
