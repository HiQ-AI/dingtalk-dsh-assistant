# 验收计划

- 参数包含错误字段时返回 `invalid-arguments` 和精简 `issues`。
- 相同路由请求重复提交返回持久化回执。
- 路由请求被替换或消息版本变化时返回当前请求信息。
- 回复成功后再次提交，以及模拟运行层重启后再次提交，均返回同一个 Outbox 回执且不新增消息。
- 未知回复请求返回 `request-unavailable`，不产生 Outbox。
- 嵌套 `undefined` 可安全省略，不可序列化输出明确失败。
- Task 结果错误只报告匹配分支的问题，不输出联合类型全分支错误。
