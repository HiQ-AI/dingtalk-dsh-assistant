# 验收计划

本计划实施 `docs/spec/task-owner-session-orchestration.md`。先采集旧行为反例，再逐子目标修改并运行直接相关功能测试；跨模块闭环使用隔离控制库与模拟平台回执。生产审批、真实 UAT 发布和钉钉发送仅在对应既有业务授权下验证，不为制造用例发起真实操作。

结果记录为 `matrix.csv` 与每轮 `round-N.md`。失败修复另起新轮，不覆盖旧证据。全绿才写 `report.md`。验收区分代码测试、数据库迁移、本地安装和真实渠道业务行为。
