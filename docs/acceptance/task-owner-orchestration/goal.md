# 任务负责会话与多流程执行

> 状态：ACTIVE
> Goal ID：task-owner-orchestration
> 最近维护：2026-09-25T20:20:00+08:00
> 权威目标：`D:/project/worktrees/dingtalk-task-owner-runtime/docs/acceptance/task-owner-orchestration/goal.md`

## 总目标

依据 `docs/spec/task-owner-session-orchestration.md`，让每个业务 Task 拥有一个可恢复的负责会话，接纳同话题补充、组合多个专业流程或日常能力，按目标验收，并统一汇报；保留现有 Host 的持久化、权限、审批、效果对账和通知回读边界。

## 完成条件

- 设计文档 T01–T30 的适用场景均有实际验证证据，无未关闭的 FAIL。
- 任务创建、补充、取消、阶段推进、结果汇报和重启恢复运行于同一 Task 身份，接入当前运行时并完成本地功能验证。
- 迁移有无副作用自检、源/目标对账、回退边界；README、部署说明与实际行为一致。
- 代码相关的本地实跑通过，PR 状态与提交内容独立回查；本地部署与真实渠道业务验证分别记录，不用前者替代后者。

## 范围与约束

- 基线为 origin/main `0744d687be4a74517153b38e8d9b8b2a92b96c0d`，方案来自提交 `d00c2d9`；在隔离分支 `worktree-task-owner-runtime` 开发，主检出保持不动。
- 默认只跑定向功能测试；生产发布和数据变更保留真人审批。未经已有业务授权不为验证触发真实外部动作。
- 不把旧数据、未知外部效果或通知 ACK 冒充成功；新会话只管理原业务 Task，不重放已接纳动作。
- 各子目标可并行开发，集成与状态写入由本 goal 的维护者负责。所有源码修改先读当前实现。

## sub goal matrix

| ID | 子目标 | 完成判据 | 状态 | 证据 |
| --- | --- | --- | --- | --- |
| SG1 | 任务负责会话与持久事件 | 同 Task 独立会话可创建、休眠、恢复、接纳事件并调度多阶段；崩溃无重复动作 | 进行中 | 原生会话复用、缺失换代服务闭环、持久事件和验收测试通过；真实杀停点待测，见 round-2 |
| SG2 | 消息与任务变更 | 同话题输入完整覆盖，部分动作不丢，补充精确影响、任务级暂停取消有效 | 进行中 | A/B/C/E 已定向通过；报告语言只改后缀 F 待测，见 round-1 |
| SG3 | 流程与通用能力 | 排查→方案→工程→独立 UAT 可衔接；真实工程来源校验；日常能力有真实验收 | 进行中 | G/H/I 修复与适配合同通过；日常文件/平台能力及完整业务链待测，见 round-1 |
| SG4 | 人工节点与统一报告 | 方案确认及生产/数据审批绑定；Task 阶段/最终报告齐全，不误撤或重发 | 进行中 | 方案确认与阶段/最终报告定向通过；生产/数据审批版本漂移及真实回读待测 |
| SG5 | 迁移、本地验证与交付 | 迁移自检及对账、定向功能测试、文档同步、PR 与本地部署逐层回查 | 进行中 | v1→v2→v3 测试库迁移通过；本地实例、PR、部署未开始 |

## 当前检查点

- 当前子目标：SG1
- 唯一下一步：补齐真实杀停点恢复测试，随后核查审批绑定、日常能力与任务报告的剩余案例，再进行本地迁移与部署验收。
- 未闭环项：SG1–SG5 均有待验收边界，详见 `matrix.csv` 中 TODO 与 `round-1.md`。

## 进展

- 2026-09-25：方案提交并独立签出最新主干；确认节点原生会话与业务 Task 计划分离，当前 Task 计划缺任务级取消状态。建立本目标文件，尚未实施产品功能。
- 2026-09-25：完成 Task Owner/事件/验收/报告账及 schema v3 迁移、消息侧补充/编辑/优先控制、任务级控制、动态工程交接和通用流程核验；首轮组合定向测试 182/182 通过，新增用例定向复跑通过。未执行本地实例部署或真实渠道验证。
- 2026-09-25：第二轮组合定向测试 204/204 通过；取消后重新授权的版本与引用错误修复并复跑，服务层已绑定会话缺失换代闭环通过；真实本地控制库零写检查和历史备份的隔离迁移通过。活跃库仍为 v2，尚未部署。

## 重大决策

- 采用一个业务 Task 一个持久负责会话；专业流程/能力为受控执行步骤，Host 负责事实与副作用账。详见 `docs/spec/task-owner-session-orchestration.md`。
- 同 Task 的业务步骤先串行，跨 Task 并行；待真实需求出现再扩大同 Task 并行范围。

## 重要信息

- 旧 JSON v8→v9 迁移脚本不能直接处理当前 SQLite 执行控制库。
- 方案中 30 项用例是待验证清单，不是通过结果；每轮证据另存 `round-N.md`，状态以 `matrix.csv` 为准。
