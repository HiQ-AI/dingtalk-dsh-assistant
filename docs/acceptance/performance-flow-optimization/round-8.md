# 第 8 轮：原生反证修复及最终代码回归

最终 `pnpm test`：**514 PASS、0 FAIL、0 SKIP**，原输出 `round-8-tests.log`。

原生 AgentLoop / Session / ToolRuntime / DomainStore 使用确定性模型适配器，覆盖实际调度和工具运行边界；不声称验证了真实模型供应商或真实钉钉投递。

## 反证及修复

- `setImmediate → cancel` 可能早于异步 `post-execute` 完成，污染已接纳提交的工具结果。终态立即关 pre-step，等待 idle 后释放；关闭时才显式取消。
- `restrict` 仅约束全局工具，不能列入 scope-local 工具。角色只注册对应业务工具，global allow 使用真实存在集合，原生单调 guard 另阻断本地工程写工具。
- pre-step 已 claim 输入后直接 reject 会丢消息。让路时复用原生 Inbox prepend 门禁；恢复原请求后原消息恰好消费一次。
- 审阅句柄创建期间可能到达撤销路由。启动模型前重查队列，让路由优先，保持同群模型串行。
- 当前消息及其引用命中的 Task 可能关联多个 Topic；路由关联摘要包含这些关系，按预算分页，不只保留被引用单项。

## 已验证边界

恢复预检失败不改任务/群、不新建会话；旧版本、待授权和待归类撤销拒绝恢复；旧 Agent、跨群、跨请求和错误角色不能提交；连续流片段不放大首流写入；超过 500 调用、重启、seed/replace、跨日和缺失数据计量都有回归。

## 仍未通过的验收目标

- 真实流量时延、token/缓存收益、首次实质回复人工标注、81 条语义路由人工真值尚未验证。
- 上游 glob 补丁仅本地提交与补丁交付；4 条宽 worktrees 查询仍超时，8 条无固定前缀不适用剪枝，整体低于 5% 目标未达成。
- 受限资源入口明确不支持 PDF、Office、音视频及需登录文档；不得将这些资源的内容标成已读。不能以本轮文本/图片测试声称全部格式可用。
- 未合并、未发布、未安装到本机运行 profile；PR/CI 结果在交付记录另行回读。

因上述验收边界，保留 PENDING/FAIL，不生成“全部目标达成”的 report.md。
