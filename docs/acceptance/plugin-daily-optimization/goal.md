# 插件逐项优化目标

总目标：按 `docs/spec/plugin-daily-review-20260921.md` 的证据和优先级修复当日问题，并分别验证。当前在隔离 worktree `worktree-plugin-daily-optimization` 开发；不改主仓检出，不自动重放真实群消息、审批或业务数据写入。

| 子目标 | 状态 | 完成条件 |
| --- | --- | --- |
| 1. 内部审阅故障停止空转 | 源码完成，待真实运行验收 | 同因故障不会反复驳回/重建；Host 持久保留报告、通知故障并可安全恢复；业务驳回仍可改稿 |
| 2. 派发前范围与流程校验 | 源码完成，待运行验收 | 独立事项、Agent 自身交付、条件流程和清单机械缺失在派发时可核对 |
| 3. 补充消息与阶段进度 | 源码完成，待运行验收 | 无影响输入保留进度；受影响审阅重排；授权变化阻断旧审批；事项完成可独立反馈 |
| 4. 工具/通知观测与统计 | 源码完成，待完整测试及运行验收 | 正确关联工具结果与错误；发送和回读可区分；明细裁剪后统计可核验 |
| 5. 活动写入恢复和健康 | 源码完成，待运行验收 | 临时写盘失败可以补齐并消除当前故障；持续故障可见且不误清除 |
| 6. 交付 | PR 已创建；本机 Profile 未切换 | 相关测试、文档更新、PR 与状态回读完成；真实运行验收在安装后单独记录 |

当前决定：先处理子目标 1，再顺序推进。沿用现有 Task report 队列、Outbox、活动投影和阶段模型，避免另建并行状态。`method-select` 等当前真实任务不作为代码测试的重放输入。

证据起点：2026-09-21 当日审计见 `docs/spec/plugin-daily-review-20260921.md`；PR #110 已合并，当前 worktree 基于最新 `origin/main` 98f63bd。

本轮交付：提交 `4127560`、`8f68b6c`，`pnpm test` 470/470；PR #111 已回读为 OPEN，base `main`、head `worktree-plugin-daily-optimization`。当前未安装本机运行 Profile，真实群消息与业务数据不作为测试重放。`gh pr checks` 暂无报告的检查，不等同于 CI 通过。

部署验收续修：PR #111 已合并为 `25a3429`。首次本机安装后发现历史已完成 Task 的 Session 不存在，被新审计误计为当前 `activity-projection` 故障；当前任务 61/61 completed。续修在独立 worktree `worktree-activity-audit-history` 完成后才替换本机包，保留首次安装包与停机前存储备份用于追溯。
