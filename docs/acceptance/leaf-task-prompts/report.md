# 叶子任务提示词库验收报告

Round 1 全部用例通过。插件现在将叶子通用规范与任务流程库分开：叶子常驻轻量索引，读取正文后持久化流程 ID 与修订；动态系统提示在后续请求、上下文压缩和 Session 恢复时按 Task 引用重新注入当前正文。目标或授权变化会清空旧选择，流程被修改、停用或删除后旧选择不能提交完成结果。

设置页支持流程新增、编辑、启停和移除，配置写入使用集合版本拒绝陈旧覆盖。旧配置无需迁移即可读取为空流程库，原通用提示词保持不变。

完整自动测试 255/255 通过，生成产物确定，前端 strict 静态审计零发现。提交 `88e1e12` 的 tgz 已部署到本机 Web profile；运行端口、DWS bridge 和活动 Task 恢复均通过回读。浏览器 smoke 验证真实设置页交互，未写入临时配置数据。
