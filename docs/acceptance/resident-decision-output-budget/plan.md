# 常驻决策输出收敛验收

症状：单个确认事项的 `group_topic_route_submit` 返回 54,270 字节，重复包含 32 个历史事项片段和 54 项归属映射。根因、协议和实施边界见 `../../spec/resident-decision-output-budget.md`。

验证按路由回执、决策首屏、来源去重、分页已读、压缩、授权、状态快路径和本机安装分开记录。源码与打包验证不能替代已安装版本或真实群消息验证；本机有活动 Task 时不重启服务。
