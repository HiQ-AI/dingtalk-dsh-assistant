# 话题页面重构验收报告

Round 1、2 全部用例通过。话题页已形成稳定的窄索引与宽详情结构；左侧只展示名称与摘要，不显示话题状态、版本或进度横条。右侧明确分开展示标题和话题摘要，引用消息默认折叠。新 Topic 使用对齐 Task 名称的简短标题并由 `summary` 承载细节；已有摘要的历史长标题由 Resident AI 重新概括，不做字符截断。桌面与 390px 视口均无横向溢出，固定版本后台读取、分页、任务跳转、键盘访问、错误恢复和异步竞态保护保持可用。

验收范围包括源码、自动化测试、严格静态审计、独立 Edge 浏览器 fixture，以及本地 DSH Web 真实 Resident 数据页面回读；未部署到远程环境。
