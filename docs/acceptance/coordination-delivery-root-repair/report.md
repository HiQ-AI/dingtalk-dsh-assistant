# 协调与交付根治验收报告

## 结论

2026-09-11 审计的 22 项问题已逐项复验并全部通过。最新 main 原有 18 项已经闭环；本次修复其余 4 个共享根因：回复路由不一致、协调请求无失效终态、恢复问题无限累积、人工批准范围可被备注改写。

本次没有把 Woodpecker、Docker、GitHub PR 或部署步骤写入通用叶子机制；开发部署流程仍只作用于明确选择该流程的任务。

## 验证摘要

- 定向回归：199/199 PASS。
- 全量回归：419/419 PASS，0 fail，0 skipped。
- Web client 生成与 `git diff --check`：PASS。
- 正式 profile 包安装、源码/安装制品哈希回读：PASS。
- 运行健康：由 degraded / 116 个重复恢复问题恢复为 ok / 0 个当前恢复问题；DWS 入站健康。
- 真实页面：运行看板、任务页、人工介入列表、详情弹窗均通过，页面错误为 0。

## 保留边界

- 没有重放历史钉钉消息，也没有把历史 pending 人为改写为 sent；因此不声称真实外发已重新投递。
- schema 仍为 v7，但新增了可选字段与 `superseded` 终态；若回滚到不认识该枚举的旧二进制，不应继续写同一存储，应优先前向修复。
- 验收覆盖当前失败样例及相邻路径，不把有限测试表述为对所有未来自然语言判断的绝对证明。
