# 叶子按自身职责完成任务

总目标：消息要求当前 Agent 完成交付、再由他人检查时，当前 Task 在自身交付及必要自验证完成后独立结束；真实前置依赖仍受控等待。历史状态可恢复读取，不重发过时检查。

| 子目标 | 当前状态 | 证据或下一步 |
| --- | --- | --- |
| 明确职责边界和影响链路 | 已完成 | `docs/spec/leaf-owned-work-completion.md` |
| 修正任务范围、报告契约、等待审阅和历史读取 | 已完成 | Runtime/Store/Topic 测试与本轮 diff |
| 自动测试和在线只读审计 | 已完成 | `matrix.csv`、`round-1.md` |
| PR 审阅和合并 | 待执行 | 先核对 PR state 和 required checks |
| 本机切换及运行态验证 | 等待安全切换窗口 | 在线仍有别的运行 Task，不中断该任务 |
| 隔离真实模型语义与真实群消息验证 | 待执行 | 不以预设模型输出或 Outbox 回执冒充业务验证 |

关键决策：`coordination` 只保留历史读取；新等待先审阅本 Agent 未完成的交付及必要依赖，审阅为范围误扩时使用原消息来源和受影响阶段做版本化修订。已有已完成任务不重跑。
