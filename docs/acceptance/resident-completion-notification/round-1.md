# Round 1 验证记录

## 结论

完成通知快路径、并发失效保护、失败回退、上下文限额和投递时间记录均已通过自动化验证。

## 证据

- `npm test`：268 个测试通过，0 个失败。
- 正常完成路径：完成验收返回通知草稿，Task 原子完成后直接写入 Outbox，未创建第二个 `TASK_COORDINATION` 请求。
- 并发保护：待处理消息变化、候选话题集合变化时不会提交旧草稿；通知写入失败会进入原协调流程。
- 上下文边界：模型仅收到最近 20 条消息，序列化消息总长度不超过 12000 字符。
- 投递可观测性：Outbox 确认发送时写入 `deliveredAt`。
- 遥测隔离：通知已入 Outbox 后，即使执行事件写入失败也不会再次发起通知协调。
- 本地部署：安装本轮 tgz 后，`runtime.js`、`topic-runtime.js`、`store.js`、`fake-llm.js` 与工作区源码 SHA256 均一致。
- Web：携带启动日志生成的认证 URL 访问 3080 返回 HTTP 200；18998 Runtime 可访问。
- 配置：任务流程配置仍为版本 3、共 10 项，部署未覆盖用户配置。
- DWS：群消息监听和人工回复监听均为 `ready`。启动历史补拉仍返回既有外部命令错误 `dws_read_failed:1`，因此 `/health` 为 `degraded`；该错误发生在未改动的 DWS 读取链路，不影响本轮本地代码装载和自动化验收，但本轮不把真实群消息收发声明为已通过。
