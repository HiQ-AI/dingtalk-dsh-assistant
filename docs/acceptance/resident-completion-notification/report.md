# 常驻会话完成通知优化验收报告

## 验收结果

自动化用例全部通过。正常完成路径从“完成验收请求 + 通知协调请求”收敛为一次常驻会话请求，并保留并发失效、失败回退和重启补偿机制。

## 范围

- 合并完成验收与群聊通知草稿生成。
- Task 完成后由 Host 校验版本并提交通知。
- 限制注入模型的近期消息数量和字符数。
- 记录完成链路事件和 Outbox 实际投递时间。

## 验证

- `npm test`：268/268 通过。
- 本地 tgz 已装入 DSH Web profile，关键源码哈希一致，认证页面返回 HTTP 200，任务流程配置版本 3 的 10 项配置保持不变。
- 实时消息监听已恢复；DWS 启动历史补拉因外部命令返回 `dws_read_failed:1` 处于 degraded，本轮未执行会向真实群发送消息的 E2E。
- 详细用例状态见 `matrix.csv`，过程证据见 `round-1.md`。
