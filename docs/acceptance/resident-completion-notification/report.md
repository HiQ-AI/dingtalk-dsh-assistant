# 常驻完成通知与流程一致性验收报告

当前结果以 Round 2 为准：本轮代码修复、自动化与本地部署验证通过；真实 DWS 投递由用户明确暂缓，不在通过结论内。

## 当前结果

- 保留一次完成审阅及通知草稿生成，Task 完成后才入队；并发变化回退重审。
- 组织未授权不再绕过回读；流程版本、异常报告、重启恢复与并发暂停均有确定性门禁。
- 审阅和通知回退均限制为 40k 信封，相关消息不超过 20 条/12k，超限内容按固定快照分页；多流程和无匹配均支持。
- 流程索引和选择依据供语义复核，版本门禁不等于模型语义绝不遗漏。

## 验证

- `npm test`：304/304 通过，0 失败、0 跳过；Web Client 构建与 diff 检查通过。
- 独立合成浏览器：7 种发件状态、键盘筛选和窄屏长错误展示通过，无真实 DWS 请求。
- 已安装源码提交 d9217a6 的 Assistant/Observer tgz，7 个修改文件 SHA256 一致；profile patch 与 Agent 配置摘要未变，保留 10 项流程、配置版本 3。
- 新进程同时监听 3080/18998；HTTP 200，认证浏览器可打开运行看板，无页面错误或 API 认证失败。
- PR #86 OPEN，已回查远端代码提交。

## 边界

`/health` 仍 degraded，历史补拉错误 dws_read_failed:1，恢复问题数为 0，监听为 ready。未重新登录、未强制补发消息，不声称真实发送与回读成功。

Round 1 的上下文限额证据只覆盖多条短消息；Round 2 补足单条长消息及整个请求边界，并覆盖改版、停用、删除重建、恢复与暂停竞态。详见 matrix.csv、round-2.md 和 scripts/verify-outbox-ui.mjs；历史过程保留在 round-1.md。
