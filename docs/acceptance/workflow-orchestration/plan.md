# 实施与取证计划

实施依据：docs/spec/workflow-orchestration-contracts.md。执行进度以同目录 goal.md 为准，用例状态以 matrix.csv 为准。

先验证契约和错误分类，再接入结构化计划及 v9 迁移、可靠中转、确定性检查、执行许可和公平调度。每批使用现有测试文件扩展真实行为反例；跨进程和真实 SDK 单列取证。全部本地通过后创建 PR；部署和真实消息发送不包含在本次本地测试授权中。

风险重点：schema 丢字段、跨版本旧会话、操作未知结果、取消在途写入、通知补发、Goal 恢复绕过执行许可。每轮记录实际命令和结果，不把 mocked 外部系统当真实 E2E。
