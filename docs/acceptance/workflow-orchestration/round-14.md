# 第 14 轮：全仓回归发现状态问答边界失配

`pnpm test`：612 项，611 通过，1 失败。

失败用例为“越界Task动作、额外字段和未知来源均不提交”。状态问答用 `groupDecisionSchema.options[0]` 隐式选取纯回复分支；主决策的等价分支重构后，该下标分支已包含动作，导致只读限制失效。保持反例不变，将只读契约改为显式验证空 actions 与非空 reply。全仓搜索未发现其他按此下标选择分支的调用方。

本轮不允许部署；第 15 轮重新执行全量验证。
