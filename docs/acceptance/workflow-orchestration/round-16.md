# 第 16 轮：部署发现的路由续行空转与修复

首个部署候选 85aa27a 安装后健康接口先可读、随后超时，Web 未完成就绪。对已核实进程只读抓取 Inspector 栈，定位 coordination-sessions 的队列循环：路由量子续行进入队尾，旧取队逻辑只看队首，非路由请求检测到后方路由后又插回队首，造成微任务空转。

60f883c 改为明确选择路由项索引，仍保留两次路由后非路由公平机会。新增“路由量子续行排到决策之后”回归；21 项队列与原生生命周期测试通过。

`pnpm test` 全仓 **613/613 通过**，0 失败、0 取消、0 跳过，50.62 秒；输出见 [full-tests.txt](round-16/full-tests.txt)。第二次部署保留当前业务数据并再次备份，未恢复旧快照。Inspector 随首个故障进程退出，不作为正式启动参数。

最终部署包：Assistant 0.5.15 本地修复包，源码 60f883cfeae9020c7a9a78fac3a2274c371efb64，SHA256 `ef42e996500161631264b4ef672846860fa193d18c6a8608a8cea1b39af3bcfc`，34 个 JS 文件安装摘要一致。运行态验收见后续部署回读。
