# 第18轮：决策上下文与协调积压补修

## 本地结果

- 最终 `node --test`：623/623 PASS，0 fail/cancel/skip，51.37秒；完整输出 round-18/full-tests-final.txt。首轮622/622通过后追加文件卡片反例，再全量执行。`node scripts/build-web-client.mjs` 无生成差异，`git diff --check` 通过。
- 决策首包流程目录、合法section枚举、流程批量读取及版本过期重排；读取后改版/禁用的真实new-task提交拒绝，Task/Outbox/Decision零副作用。内联消息不重复读。
- 原生AgentLoop中正常读取/审阅/提交连续三步只获槽一次；旧九次失败请求仍让出，路由抢占、取消、失效与post-execute结果落稳保护通过。同群模型峰值仍1。30秒仅边界预算，不打断正在执行的模型步骤。
- 原生Domain中30个同会话计量事件合并一次写入；写序列为s1计量→groups业务→s2计量→s3计量。Promise写后完成、失败可重试、close排空、去重回调再入通过。
- DWS卡片仅容许固定下载尾注差异，精确文件ID/文件名必须匹配；不同ID、改正文、缺资源及普通文本相同尾注均拒绝。测试使用合成文件ID。

## 现场慢调用证据与解释边界

- 无动作/无回复决策：09:21:03.879Z tool/call，09:22:18.040Z decision.createdAt，09:22:57.262Z coordination更新，09:23:00.662Z tool/result，总116.783秒；业务动作不在此调用内执行。
- 另一提交41.630秒；routing-required回执也耗18.979秒。
- 已安装SDK和工作区对应SDK摘要一致；Domain全部表共用enqueue，single-json每次put重写约21MB整库并同步落盘。计量每事件put与业务竞争，故采用单计量写入器与等待批次合并。
- 未采集现场SDK写队列内每段耗时，不能把全部116.783秒归于计量，更不能宣称真实时延已下降。未发现活动投影无限恢复的证据，不改其语义。

## 真实业务与部署边界

17:40旧包回读：#1336、#1353、#1354已创建running Task；#1371已重开queued；#1350旧决策blocked，#1372尚无Task。这些推进发生在本次包部署之前。没有手工补建、修改真实存储或发送测试消息。runbook要求活动任务安全结束前保留实例，部署状态另行回读。
