# 第 13 轮：群消息协调延迟回归

针对现场 #1336 的脱敏等价图文请求，修复 strict 联合分支投影、字段反馈、同群公平调度和路由等待草稿重验。

- 原生 AgentLoop/Session/ToolRuntime + Store/Coordinator 回放：前一话题九次无效提交尚未结束，新图文 Task 已创建；Task、applyAction、Outbox 各一次，公平让出不增加 attempt。
- Topic 专项 102 项通过，覆盖同话题撤销使旧草稿失效、候选变化需重新审阅、缓存接纳后协调账 completed。
- Runtime 152 项通过，未路由输入阻止叶子许可，Decision 完成后恢复，重复 pump 不重复恢复或派发。
- 初次针对性测试 120/122：两项旧断言要求先清空路由才准备决策，与新行为不符；更新断言并增加提交零副作用反例后通过。
- 审阅发现缓存绕过包装层后协调账仍 pending，已统一终态写回并增加回归。

此轮不是最终全仓验收；真实 DWS/模型/图片识别不在确定性回放范围。
