# 第15轮：发布前反例和本地切换

## 已执行验证

- `node scripts/build-web-client.mjs`：exit 0，生成内容与已跟踪页面一致。
- `node docs/acceptance/runtime-redesign/scripts/check-legacy-reconciliation.mjs <legacy-json> <private-manifest>`：5项通过；原对象及全部Task保持不变；过期回执不标送达；数量、记录时间、正文或完成证据不匹配时拒绝。
- 修订输入增加约束的反例首次失败：I上下文6317字节超过6000字节。根因是binding中重复传入同一target卡。改为无损单份投影，预算不变，有效目标/权限/原文/约束未删除；消息与服务29项通过。
- 修订输入保留原约束并合并新增约束，不因补充消息未复述而清除限制。
- 工程验证取消/超时反例：真实父子Node进程退出，后续步骤未运行；终止失败不报告排空。

## 当前状态

完整回归两轮通过：第一轮804/804，第二轮在代际/检查排空补充后806/806，0失败/0跳过，第二轮127985ms。工程续代与重启反例见round-16.md。
本地实例尚未停止，旧账未写入，未安装包；不能将本记录解释为已部署。

## 切换授权

用户明确同意保全快照后，将五条已有后续处理的旧协调请求标记被取代，并终止一条超时旧回执的补发。身份和逐项证据仅保存在本机私有manifest，不随PR公开。
