# 任务流程一致性门禁验收报告

## 结果

计划和完成审阅现在使用叶子当前选择的同一份任务流程。流程正文按审阅请求加载，避免全部常驻注入；原始消息、流程组合、计划和结果形成一致性门禁。

## 生效行为

- 主会话负责忠实表达原始业务目标，不把自身建议扩写为用户要求。
- 叶子计划声明流程组合、复用证据、不适用步骤及有原始消息依据的例外。
- 冲突计划可被结构化拒绝，重新规划前不能推进或完成。
- 流程修订只影响引用该流程的活动任务，历史检查点与证据仍保留。

## 验证

- `npm test`：271/271 通过。
- 用例与证据见 `matrix.csv` 和 `round-1.md`。
- 本地 profile 已安装本次 tgz，关键文件哈希与源码一致；同一 Node 进程监听 3080/18998，流程配置仍为版本 3、10 项。

## 未通过边界

真实 DWS 回补仍报 `dws_read_failed:1`，使整体健康保持 `degraded`。群和人工回复 listener 已连接，但本轮没有发送真实群消息，因此该项保留失败，不能作为业务 E2E 通过证据。
